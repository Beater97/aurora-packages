kind: flowservice
id: jsonplaceholderWSDL.flows.convertNumber
name: Convert Number to Words (placeholder)
pipeline:
  - map:
      mappings:
        - expression: "${request.body.number}"
          to: output.input_number
        - value: "SOAP integration TBD — requires common.services.soap.call"
          to: output.message
