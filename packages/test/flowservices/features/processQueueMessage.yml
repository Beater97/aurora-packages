kind: flowservice
id: test.features.processQueueMessage
name: Process Queue Message (test consumer)
input:
  message:
    type: object
    required: false
pipeline:

  - map:
      mappings:
        - expression: "${input.message}"
          to: output.received
        - value: "processed"
          to: output.status
