kind: rest_resource
id: test.rest.retryYaml
name: Test YAML-only retry (no Python service)

routes:
  - method: GET
    path: /test/retry-yaml
    flowservice: test.features.testRetryYaml
    response:
      status: 200
