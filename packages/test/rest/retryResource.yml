kind: rest_resource
id: test.rest.retry
name: Test Retry Policy

routes:
  - method: POST
    path: /test/retry
    flowservice: test.features.testRetry
    input:
      fail_until: request.body.fail_until
    response:
      status: 200
